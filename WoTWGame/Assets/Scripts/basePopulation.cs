using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class basePopulation : MonoBehaviour {
    public float up1, up2, down1, down2, pop, biomass, corruptedPop, corruptedBiomass, speed, size, startSize, startSpeed, rate, startRate;
    public int sizeMod, speedMod, toughMod, timesSpeedChanged;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
